services:
  sorteos.commands:
    class: Drupal\sorteos\Commands\SorteoCommands
    arguments: ["@sorteos.importer_manager", "@lock", "@sorteos.logger.channel.sorteos"]
    tags:
      - { name: drush.command }
