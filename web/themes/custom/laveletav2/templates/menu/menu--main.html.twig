    {% for item in items %}
      {% if item.in_active_trail %}
        {{ link(item.title, item.url, { 'class':['main-menu__item active'] }) }}
      {% else %}
        {{ link(item.title, item.url, { 'class':['main-menu__item'] }) }}
      {% endif %}
    {% endfor %}
